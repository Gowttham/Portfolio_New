{
  "name": "portfolio-mern",
  "version": "1.0.0",
  "description": "Gowttham S – Full Stack Portfolio (MERN)",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "dev": "start \"Frontend\" cmd /k npm run dev --prefix frontend && start \"Backend\" cmd /k npm run dev --prefix backend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
